from django.apps import AppConfig


class SignaturesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.signatures"
    verbose_name = "Digital signatures (ICP-Brasil)"
