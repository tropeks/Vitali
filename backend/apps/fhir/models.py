# The FHIR module deliberately ships no models — it is a stateless resource
# surface over existing tenant data. Keeping this file in place so Django's
# app loader does not warn about a missing models module.
