from django.apps import AppConfig


class SmartSchedulingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.smart_scheduling"
    verbose_name = "Smart Scheduling (slot ranker)"
