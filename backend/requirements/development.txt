-r base.txt

# Dev tools
ipython==8.32
django-extensions==3.2
Werkzeug==3.1

# Dependency lockfile tooling — regenerate requirements/production.lock with
# `pip-compile --generate-hashes` (see docs/DEVELOPMENT.md → Dependency lockfile).
pip-tools==7.5.3

# Testing
pytest==8.3
pytest-django==4.10
pytest-cov==6.0
factory-boy==3.3
faker==33.3

# Linting (managed via pyproject.toml/ruff)
ruff==0.9
mypy==1.15
django-stubs==5.1
djangorestframework-stubs==3.15

# Import boundary enforcement (P1-01 — domain-independence contract)
import-linter==2.1
