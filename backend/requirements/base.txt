Django==5.2
djangorestframework==3.16
django-tenants>=3.5,<4.0
django-environ==0.12
djangorestframework-simplejwt==5.5
django-encrypted-model-fields==0.6.5
django-filter==24.3
redis==5.2
celery==5.5
django-celery-beat==2.8
psycopg2-binary==2.9.10
drf-spectacular==0.28
argon2-cffi==23.1
gunicorn==23.0
lxml>=5.0
requests>=2.32
jinja2>=3.1
anthropic>=0.40
openai>=1.0
# S-062: MFA (TOTP)
pyotp>=2.9
qrcode[pil]>=7.4
# S-065: Prescription PDF
weasyprint>=62.0
# ICP-Brasil chain-of-trust validation (apps.signatures). cryptography is used
# for PKCS#12 loading + DER serialisation; pinned >=42 for the *_utc accessors.
cryptography>=42
# RFC 5280 certificate path validation (apps.signatures.services.chain). Replaces
# a hand-rolled path builder that skipped intermediate validity windows,
# pathLenConstraint, CA keyCertSign, NameConstraints, and weak-algorithm
# rejection. Pulls asn1crypto. oscrypto>=1.3.0 needed for OpenSSL 3 (python:3.12).
pyhanko-certvalidator>=0.26
oscrypto>=1.3.0
# OpenTelemetry (P2-03 / P2-01) — default OFF; imported lazily inside
# vitali/observability.py. These must be present in the image when OTEL_ENABLED=true.
opentelemetry-api==1.41.0
opentelemetry-sdk==1.41.0
opentelemetry-instrumentation-django==0.62b0
opentelemetry-instrumentation-celery==0.62b0
opentelemetry-instrumentation-psycopg2==0.62b0
opentelemetry-instrumentation-requests==0.62b0
